import React from "react";
import Styles from "./sports-card.styles.less";

export const SportsCard = ({ marketId, markets, ammExchanges, timeFormat }) => {
  return <article className={Styles.SportsMarketCard}></article>;
};
